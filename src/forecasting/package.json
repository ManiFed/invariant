{
  "name": "invariant-forecasting-service",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix ../.. run build:forecasting",
    "start": "npm --prefix ../.. run start:forecasting",
    "dev": "npm --prefix ../.. run dev:forecasting"
  }
}
